{
  "name": "route",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --harmony server.js",
    "postinstall": "[ ! -f config.json ] && cp config.example.json config.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "co-request": "^0.2.0",
    "dockerode": "~2.0.0",
    "empty-port": "0.0.1",
    "http-proxy": "^1.2.0",
    "koa": "^0.9.0",
    "koa-static": "^1.4.6",
    "thunkify": "^2.1.2"
  }
}
